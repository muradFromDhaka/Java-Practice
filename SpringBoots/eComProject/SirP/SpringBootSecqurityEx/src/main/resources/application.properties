spring.application.name=SpringBootSecqurityEx

# JWT Configuration
app.jwt.secret=ThisIsAVeryLongAndStrongSecretKeyAtLeast64CharactersLongForHS512AlgorithmSecureUseOnly
app.jwt.expiration=86400000

# Database Configuration (update with your values)
spring.datasource.url=jdbc:mysql://localhost:3307/securityDb
spring.datasource.username=root
spring.datasource.password=root

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

file.upload-dir=uploads
